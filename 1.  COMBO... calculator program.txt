Below is your **COMPLETE SPPU OOPL PRACTICAL EXAM VIVA + LINE-BY-LINE DEEP THEORY PREPARATION** for your **Java Calculator program**.
This is written in **external examiner + viva + practical journal + interview level format**.

---

# 🧠 PART 1: EXTREME VIVA / ORAL PREPARATION

---

# ⭐ A. BASIC CONCEPTUAL QUESTIONS

### Q1. What is this program?

**Ans:**
It is a **menu-driven calculator program** that performs arithmetic operations using Java concepts like Scanner, switch-case, loop, and exception handling.

---

### Q2. Why is class used?

**Ans:**
A class is used to group data and methods together. It acts as a blueprint for program structure.

---

### Q3. Why object is not created for Calculator?

**Ans:**
Because all logic is inside `main()` method which is static. So object is not required.

---

### Q4. Why Scanner is used?

**Ans:**
Scanner is used to take **runtime input from user (keyboard input)**.

---

### Q5. Why static main method?

**Ans:**
Because JVM calls main method without creating object.

---

# ⭐ B. OOP CONCEPT QUESTIONS

---

### Q6. Is inheritance used in this program?

**Ans:**
❌ No inheritance is used. Only single class exists.

---

### Q7. What is inheritance?

**Ans:**
It is a mechanism where one class acquires properties of another class using `extends`.

---

### Q8. Why extends keyword is not used here?

**Ans:**
Because there is no parent-child relationship between classes.

---

### Q9. What is polymorphism?

**Ans:**
Ability of one method to behave differently based on input.

---

### Q10. Is polymorphism used here?

**Ans:**
❌ Not directly. But switch-case shows runtime decision behavior.

---

### Q11. Why method overloading is not used?

**Ans:**
Because same functionality is handled in single method using switch-case.

---

### Q12. Why abstraction is not used?

**Ans:**
No abstract class or interface is required for simple calculator logic.

---

# ⭐ C. TRICKY EXTERNAL EXAMINER QUESTIONS

---

### Q13. What happens if extends keyword is removed?

**Ans:**
No effect here because inheritance is not used.

---

### Q14. What happens if Scanner is not used?

**Ans:**
Program becomes static and cannot take user input.

---

### Q15. What happens if object is not created?

**Ans:**
No effect because program uses static main method.

---

### Q16. What happens if main method is missing?

**Ans:**
Program will not execute → JVM error.

---

### Q17. What happens if String equals() is replaced with ==?

**Ans:**
Incorrect comparison → logical error.

---

### Q18. What happens if Math.pow() is removed?

**Ans:**
Power operation will not work.

---

### Q19. What happens if try-catch is removed?

**Ans:**
Program may crash during runtime errors.

---

### Q20. What happens if Scanner is not closed?

**Ans:**
Memory leak may occur (not critical in small programs).

---

### Q21. Why String is immutable?

**Ans:**
For security, memory optimization, and thread safety.

---

### Q22. What happens during runtime memory allocation?

**Ans:**
Variables stored in stack, Scanner object in heap.

---

# ⭐ D. JVM / JDK / JRE QUESTIONS

---

### Q23. What is JVM?

**Ans:**
Java Virtual Machine executes bytecode.

---

### Q24. What is JDK?

**Ans:**
JDK = JRE + development tools (compiler, debugger).

---

### Q25. What is JRE?

**Ans:**
JRE provides runtime environment to run Java programs.

---

### Q26. What happens during compilation?

**Ans:**
.java file → compiled into .class (bytecode)

---

### Q27. What JVM does?

**Ans:**
Loads bytecode and executes it line by line.

---

# ⭐ E. MEMORY-BASED QUESTIONS

---

### Q28. Stack memory usage?

**Ans:**
Stores method calls, variables like a, b, opr, choice.

---

### Q29. Heap memory usage?

**Ans:**
Stores Scanner object.

---

### Q30. What is garbage collection?

**Ans:**
Automatic removal of unused objects.

---

# ⭐ F. INTERVIEW LEVEL QUESTIONS

---

### Q31. Why methods are used?

**Ans:**
To divide program into reusable blocks.

---

### Q32. Why method calling is needed?

**Ans:**
To execute logic written inside method.

---

### Q33. Why class is important in Java?

**Ans:**
Because Java is fully object-oriented.

---

### Q34. Why access modifier is public?

**Ans:**
So JVM can access main method.

---

### Q35. Why private is not used?

**Ans:**
Because data needs to be accessible inside program.

---

# ⭐ G. ERROR FINDING QUESTIONS

---

### Q36. Error if semicolon missing?

**Ans:**
Compilation error.

---

### Q37. Error if braces missing?

**Ans:**
Syntax error.

---

### Q38. Error if Scanner not imported?

**Ans:**
Compilation error: cannot find symbol.

---

# ⭐ H. WHAT IF MODIFICATION QUESTIONS

---

### Q39. What if do-while is replaced with while?

**Ans:**
Program may not execute at least once.

---

### Q40. What if switch is replaced with if-else?

**Ans:**
Same output but less optimized.

---

# 🧠 PART 2: DETAILED LINE-BY-LINE CODE EXPLANATION

---

# ⭐ LINE 1:

```java
import java.util.Scanner;
```

### Meaning:

Imports Scanner class.

### Purpose:

To take input from user.

### Internal Working:

JVM loads Scanner class from Java library.

### If removed:

Compilation error.

---

# ⭐ LINE 2:

```java
public class Calculator
```

### Meaning:

Defines class named Calculator.

### Purpose:

Organizes program structure.

### Memory:

Class stored in method area.

---

# ⭐ LINE 3:

```java
public static void main(String[] args)
```

### Meaning:

Entry point of program.

### Why used:

JVM starts execution from here.

### Memory:

Stack frame created for main.

---

# ⭐ LINE 4:

```java
Scanner sc = new Scanner(System.in);
```

### Meaning:

Creates input object.

### Memory:

Object stored in heap.

### If removed:

No user input possible.

---

# ⭐ LINE 5:

```java
String choice;
```

### Meaning:

Stores yes/no input.

### Memory:

Stored in stack.

---

# ⭐ LINE 6:

```java
try {
```

### Meaning:

Start exception handling block.

### Purpose:

Prevents runtime crash.

---

# ⭐ LINE 7:

```java
do {
```

### Meaning:

Start loop execution.

### Purpose:

Repeat calculator.

---

# ⭐ LINE 8:

```java
int a = sc.nextInt();
```

### Meaning:

Input first number.

### Memory:

Stored in stack.

---

# ⭐ LINE 9:

```java
int b = sc.nextInt();
```

Same as above for second number.

---

# ⭐ LINE 10–15:

Menu display statements

### Purpose:

User interface display.

---

# ⭐ LINE 16:

```java
int opr = sc.nextInt();
```

### Meaning:

Stores user choice.

---

# ⭐ LINE 17:

```java
switch(opr)
```

### Meaning:

Decision-making control.

---

# ⭐ CASE BLOCKS:

### Case 1–3:

Arithmetic operations.

### Case 4:

Division with condition check.

### Case 5:

Modulus operation.

### Case 6:

Power using Math.pow().

---

# ⭐ LINE:

```java
Math.pow(a,b)
```

### Meaning:

Exponent calculation.

### Internal:

Uses floating-point computation.

---

# ⭐ DEFAULT:

Handles invalid input.

---

# ⭐ LINE:

```java
choice = sc.next();
```

### Meaning:

Reads continuation input.

---

# ⭐ LOOP CONDITION:

```java
while(choice.equals("yes"));
```

### Meaning:

Repeats program.

---

# ⭐ CATCH BLOCK:

```java
catch (ArithmeticException e)
```

### Meaning:

Handles division/modulus errors.

---

# ⭐ END:

Program terminates.

---

# 🧠 MEMORY FLOW SUMMARY

* Stack → variables, method calls
* Heap → Scanner object
* Method Area → class definition
* JVM → execution engine

---

# 🏁 FINAL EXAM READY SUMMARY

This program demonstrates:

* Input handling
* Decision making
* Looping
* Exception handling
* Arithmetic operations
* User interaction system
