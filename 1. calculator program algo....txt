import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        String choice;

        try {

            do {

                System.out.println("Enter First number:");
                int a = sc.nextInt();

                System.out.println("Enter second number:");
                int b = sc.nextInt();

                System.out.println("1-Addition:");
                System.out.println("2-Substraction:");
                System.out.println("3-Multiplication:");
                System.out.println("4-Division:");
                System.out.println("5-Modulus:");
                System.out.println("6-Power:");

                System.out.println("Enter no of operation do you want to perform:");
                int opr = sc.nextInt();

                switch (opr) {

                    case 1:
                        System.out.println("Addition of two numbers is:" + (a + b));
                        break;

                    case 2:
                        System.out.println("Substraction of two numbers is:" + (a - b));
                        break;

                    case 3:
                        System.out.println("Multiplication of two numbers is:" + (a * b));
                        break;

                    case 4:
                        if (b != 0) {
                            System.out.println("Division of two numbers is:" + (a / b));
                        } else {
                            System.out.println("Division of two numbers is not possible");
                        }
                        break;

                    case 5:
                        if (b != 0) {
                            System.out.println("Modulus of two numbers is:" + (a % b));
                        } else {
                            System.out.println("Modulus of two numbers is not possible");
                        }
                        break;

                    case 6:
                        System.out.println("Power of given number is:" + Math.pow(a, b));
                        break;

                    default:
                        System.out.println("Invalid");
                }

                System.out.println("Do you want to continue(yes/no):");
                choice = sc.next();

            } while (choice.equals("yes"));

        }

        catch (ArithmeticException e) {
            System.out.println("Division and Modulus is not possible");
        }
    }
}


Below is your **SPPU 2nd Year AIDS – OOPL Practical Exam Theory** for the given **Java Calculator program**. This is written in a **proper university exam format** and is ready for writing in journal/practical file.

---

# 📘 PRACTICAL EXAM THEORY

## 🧮 PROGRAM: Menu Driven Calculator using Java

---

# ⭐ 1. PROBLEM STATEMENT

The objective of this program is to design a **menu-driven calculator application** using Java that performs basic arithmetic operations like addition, subtraction, multiplication, division, modulus, and power.

The program takes two numbers as input from the user and allows the user to select an operation from a menu. Based on the selected option, the program performs the required calculation and displays the result. The program also allows repeated execution using a loop until the user decides to exit.

---

# ⭐ 2. DEFINITIONS OF CONCEPTS USED

## 1. Class

A class is a blueprint or template used to create objects.

👉 Example:

```java
class Calculator
```

---

## 2. Object

An object is an instance of a class that contains data and methods.

👉 In this program, Scanner object is created:

```java
Scanner sc = new Scanner(System.in);
```

---

## 3. Method

A method is a block of code that performs a specific task.

👉 Example:

* `main()` method → starting point of program

---

## 4. Static Keyword

Static means the method belongs to the class, not object.

👉 Example:

```java
public static void main(String[] args)
```

---

## 5. Scanner Class

Scanner is a built-in Java class used to take input from the user.

👉 Example:

```java
Scanner sc = new Scanner(System.in);
```

---

## 6. String Class

String is used to store text data like "yes" or "no".

👉 Example:

```java
String choice;
```

---

## 7. Switch Case

Switch case is used for selecting one option among multiple choices.

---

## 8. Loop (do-while)

A do-while loop executes the program at least once and repeats until condition becomes false.

---

## 9. Exception Handling

It is used to handle runtime errors using try-catch block.

---

## 10. Math Class

Math class is used for mathematical operations like power.

👉 Example:

```java
Math.pow(a, b)
```

---

# ⭐ 3. ALGORITHM (DETAILED STEPS)

### Step 1:

Start the program.
👉 Purpose: Initialize execution.

---

### Step 2:

Create Scanner object.
👉 Purpose: To take input from user.

---

### Step 3:

Declare variables:

* a (first number)
* b (second number)
* choice (to continue or stop)
* opr (operation selection)

---

### Step 4:

Start try block.
👉 Purpose: Handle runtime errors safely.

---

### Step 5:

Start do-while loop.
👉 Purpose: To repeat calculator until user exits.

---

### Step 6:

Input first number (a).
👉 Purpose: First operand.

---

### Step 7:

Input second number (b).
👉 Purpose: Second operand.

---

### Step 8:

Display menu of operations:

* Addition
* Subtraction
* Multiplication
* Division
* Modulus
* Power

👉 Purpose: User selection interface.

---

### Step 9:

Take operation choice (opr).

---

### Step 10:

Use switch-case:

* Perform selected operation
* Display result

👉 Purpose: Execute selected arithmetic operation.

---

### Step 11:

Check division/modulus condition:

* If b ≠ 0 → perform operation
* Else → show error message

---

### Step 12:

Ask user:
“Do you want to continue (yes/no)?”

---

### Step 13:

If user enters "yes", repeat loop.

---

### Step 14:

If user enters "no", exit program.

---

### Step 15:

Catch ArithmeticException if error occurs.

---

### Step 16:

Stop program.

---

# ⭐ 4. FLOWCHART (TEXT FORM)

```
START
  ↓
Create Scanner Object
  ↓
START TRY BLOCK
  ↓
DO LOOP START
  ↓
Input first number (a)
  ↓
Input second number (b)
  ↓
Display Menu
  ↓
Input operation choice (opr)
  ↓
SWITCH(opr)
  ↓
 ├─ Case 1 → Addition → Display result
 ├─ Case 2 → Subtraction → Display result
 ├─ Case 3 → Multiplication → Display result
 ├─ Case 4 → Division (check b≠0)
 ├─ Case 5 → Modulus (check b≠0)
 ├─ Case 6 → Power using Math.pow()
 └─ Default → Invalid choice
  ↓
Ask: continue yes/no
  ↓
If yes → repeat loop
If no → exit loop
  ↓
CATCH ArithmeticException (if any)
  ↓
END
```

---

# ⭐ 5. WORKING OF METHODS USED

## 1. main() method

* Entry point of Java program
* JVM starts execution from here

---

## 2. nextInt()

* Reads integer input from user

👉 Used for:

```java
int a = sc.nextInt();
```

---

## 3. next()

* Reads string input (yes/no)

---

## 4. equals()

* Compares string values

👉 Used in:

```java
choice.equals("yes")
```

---

## 5. Math.pow(a, b)

* Calculates power (a raised to b)

---

## 6. switch-case

* Selects operation based on user input

---

## 7. try-catch

* Handles runtime errors safely

---

# ⭐ 6. INPUT – OUTPUT DESCRIPTION

---

## 🔵 INPUT:

User provides:

* First number (integer)
* Second number (integer)
* Operation choice (1 to 6)
* Continue option (yes/no)

---

## 🔵 OUTPUT:

Program displays:

* Result of selected operation
* Error messages if invalid input or division/modulus by zero
* Menu repeatedly until user exits

---

## 🔵 SAMPLE OUTPUT:

```
Enter First number: 10
Enter second number: 5

1-Addition
2-Subtraction
3-Multiplication
4-Division
5-Modulus
6-Power

Enter operation: 1
Addition of two numbers is: 15

Do you want to continue (yes/no): yes
```

---

# ⭐ FINAL EXAM SUMMARY (WRITE IN FILE)

This program demonstrates the implementation of a **menu-driven calculator in Java** using fundamental concepts such as Scanner class, switch-case, loops, exception handling, and mathematical operations. It provides an interactive user interface to perform arithmetic calculations repeatedly until the user decides to stop.
