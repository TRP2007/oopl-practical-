Below is your **COMPLETE SPPU OOPL PRACTICAL EXAM + VIVA PREPARATION (HOTEL BOOKING SYSTEM)**
for **External Examiner Level (Very Important + High Scoring Material)**

---

# ⭐ PART 1: EXTREME LEVEL VIVA / ORAL QUESTIONS WITH ANSWERS

---

# 🔷 A. BASIC CONCEPTUAL QUESTIONS

### 1. What is this program?

This program is a **Hotel Room Booking System** that manages room availability using a **2D array** in Java.

---

### 2. What concept is mainly used?

* Arrays (2D array)
* Looping
* Conditional statements
* Switch-case
* Menu-driven program

---

### 3. Why Java is used?

Because Java provides:

* Platform independence
* Easy memory management
* Strong OOP support

---

### 4. What is OOP?

Object-Oriented Programming is a method where program is structured using **objects and classes**.

---

# 🔷 B. CLASS / OBJECT QUESTIONS

### 5. Why class is used?

A class is used to **define structure and behavior** of program.

👉 In this program:
`HotelBooking` class contains main logic.

✔ Without class → Java program cannot run.

---

### 6. Why object is not created here?

Because:

* Only `static main()` method is used
* No instance variables are required

👉 Scanner object is created because input is needed.

---

### 7. Why Scanner object is created?

To take **runtime input from user**.

---

### 8. What happens if Scanner is not used?

* Program cannot take input
* Booking system becomes static

---

# 🔷 C. ARRAY QUESTIONS

### 9. Why 2D array is used?

To represent hotel structure:

* Rows → floors
* Columns → rooms

---

### 10. What is `int rooms[3][5]`?

* 3 floors
* 5 rooms per floor
* Stores booking status

---

### 11. What happens if array size is wrong?

* ArrayIndexOutOfBoundsException

---

# 🔷 D. METHOD / LOGIC QUESTIONS

### 12. Why methods are used?

Methods are used to:

* Organize code
* Improve readability
* Avoid repetition

---

### 13. Why no user-defined methods?

Because program is simple menu-driven logic inside main().

---

### 14. Why switch-case used?

To handle multiple user choices efficiently.

---

# 🔷 E. OOP CONCEPT QUESTIONS

### 15. Is inheritance used here?

❌ No

Because there is no parent-child class relationship.

---

### 16. Is polymorphism used?

❌ No

Because no method overloading or overriding is present.

---

### 17. Is abstraction used?

❌ No

Because no abstract class or interface is used.

---

# 🔷 F. STATIC / MEMORY QUESTIONS

### 18. Why main is static?

Because JVM calls main without creating object.

---

### 19. What happens if static is removed?

❌ Program will not run
Error: main method not found

---

### 20. Why heap memory used?

For array storage (`rooms[][]`) and Scanner object.

---

# 🔷 G. COMPILER / JVM QUESTIONS

### 21. What is JVM?

JVM executes bytecode and converts it into machine code.

---

### 22. What is JDK?

JDK = JRE + Development tools (compiler, debugger)

---

### 23. What is JRE?

JRE provides runtime environment to execute Java programs.

---

### 24. What happens during compilation?

* Java file → bytecode (.class)

---

# 🔷 H. TRICKY EXAMINER QUESTIONS

### 25. What happens if extends keyword is used?

Not needed here → no inheritance required.

---

### 26. What happens if object is not created for Scanner?

No input possible → program fails logically.

---

### 27. Can private data be accessed?

❌ No, private members are not accessible outside class.

---

### 28. What if main signature changes?

❌ JVM will not recognize program.

---

### 29. What if Scanner is not closed?

Memory leak may occur.

---

### 30. Why String is immutable?

For security, memory optimization, and caching.

---

# 🔷 I. OUTPUT-BASED QUESTION

### 31. What is output of case 1?

Displays:

* Floor-wise room status
* Available / Booked

---

# 🔷 J. WHAT IF MODIFICATION QUESTIONS

### 32. What if 2D array becomes 1D?

Cannot represent floors properly.

---

### 33. What if rooms are not initialized?

Default value = 0 → all rooms available.

---

### 34. What if == replaced with =?

Compilation error.

---

# ⭐ PART 2: DETAILED CODE EXPLANATION (LINE BY LINE)

---

# 🔷 LINE 1:

```java
import java.util.Scanner;
```

### Meaning:

Imports Scanner class.

### Purpose:

To take input from user.

### Internal Working:

JVM loads Scanner class from Java API.

### If removed:

❌ Compilation error (Scanner not found)

---

# 🔷 LINE 2:

```java
class HotelBooking
```

### Meaning:

Defines class.

### Purpose:

Encapsulates program logic.

### Memory:

Class stored in Method Area.

---

# 🔷 LINE 3:

```java
{
```

### Meaning:

Starts class body.

### If removed:

❌ Syntax error

---

# 🔷 LINE 4:

```java
public static void main(String args[])
```

### Breakdown:

* public → accessible everywhere
* static → no object needed
* void → returns nothing
* main → entry point
* String args[] → command-line arguments

### Memory:

Stack frame created for main()

### If removed:

❌ Program cannot run

---

# 🔷 LINE 5:

```java
Scanner sc = new Scanner(System.in);
```

### Meaning:

Creates Scanner object

### Memory:

* sc → stack
* Scanner object → heap

### If removed:

❌ No input possible

---

# 🔷 LINE 6:

```java
int rooms[][] = new int[3][5];
```

### Meaning:

Creates 2D array

### Memory:

* Heap memory allocation
* Default value = 0

### If removed:

❌ No room system exists

---

# 🔷 LINE 7:

```java
int choice, floor, room;
```

### Meaning:

Declares variables

### Purpose:

Store user input

---

# 🔷 LINE 8:

```java
do
```

### Meaning:

Start loop

### Purpose:

Menu runs at least once

---

# 🔷 LINE 9:

Menu display lines

```java
System.out.println(...)
```

### Meaning:

Print output

### If removed:

❌ User cannot see menu

---

# 🔷 LINE 10:

```java
choice = sc.nextInt();
```

### Meaning:

Reads input

### Runtime:

User input stored in variable

---

# 🔷 LINE 11:

```java
switch(choice)
```

### Meaning:

Decision control

### Purpose:

Select operation

---

# 🔷 CASE 1:

Room display logic

### Internal working:

* Loop through array
* Check value

---

# 🔷 CASE 2:

Booking logic

### If condition:

Checks validity + availability

### Memory:

Array value changed from 0 → 1

---

# 🔷 CASE 3:

Cancel booking

### Memory update:

1 → 0

---

# 🔷 CASE 4:

Exit system

Stops loop

---

# 🔷 LOOP END:

```java
while(choice != 4);
```

### Meaning:

Repeat until exit

---

# 🔷 FINAL LINE:

```java
sc.close();
```

### Meaning:

Closes input stream

### Memory:

Frees resources

---

# ⭐ PART 3: MEMORY DEEP EXPLANATION

---

## 🔷 STACK MEMORY

* Stores variables like choice, floor
* Stores method calls

---

## 🔷 HEAP MEMORY

* Stores rooms[][] array
* Stores Scanner object

---

## 🔷 METHOD AREA

* Stores class definition

---

## 🔷 GARBAGE COLLECTION

* Removes unused objects

---

# ⭐ PART 4: JVM PROCESS

1. Compilation → .java → .class
2. Class loader loads program
3. Bytecode verification
4. JVM execution
5. Output generated

---

# ⭐ FINAL EXAM TIP

If examiner asks:
👉 “Why this program is important?”

✔ Answer:
It demonstrates real-world **hotel booking system using arrays and control structures in Java.**

---

If you want, I can also give:
✔ 1-page revision sheet
✔ Important 10 viva questions for last-minute exam
✔ Diagram/flowchart image
✔ OR handwritten notes format

Just tell 👍
