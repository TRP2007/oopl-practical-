import java.util.Scanner;

class HotelBooking
{
    public static void main(String args[])
    {
        Scanner sc = new Scanner(System.in);

        // 2D array for hotel rooms
        int rooms[][] = new int[3][5];

        int choice, floor, room;

        do
        {
            System.out.println("\n===== HOTEL ROOM BOOKING SYSTEM =====");
            System.out.println("1. View Room Status");
            System.out.println("2. Book Room");
            System.out.println("3. Cancel Booking");
            System.out.println("4. Exit");
            System.out.print("Enter Your Choice: ");
            choice = sc.nextInt();

            switch(choice)
            {
                case 1:
                    // View room status
                    for(int i = 0; i < 3; i++)
                    {
                        System.out.println("Floor " + (i + 1));

                        for(int j = 0; j < 5; j++)
                        {
                            if(rooms[i][j] == 0)
                            {
                                System.out.println("Room " + (j + 1) + " : Available");
                            }
                            else
                            {
                                System.out.println("Room " + (j + 1) + " : Booked");
                            }
                        }
                    }
                    break;

                case 2:
                    // Book room
                    System.out.print("Enter Floor Number (1-3): ");
                    floor = sc.nextInt();

                    System.out.print("Enter Room Number (1-5): ");
                    room = sc.nextInt();

                    if(floor >= 1 && floor <= 3 && room >= 1 && room <= 5)
                    {
                        if(rooms[floor - 1][room - 1] == 0)
                        {
                            rooms[floor - 1][room - 1] = 1;
                            System.out.println("Room Booked Successfully!");
                        }
                        else
                        {
                            System.out.println("Room Already Booked!");
                        }
                    }
                    else
                    {
                        System.out.println("Invalid Room Number!");
                    }
                    break;

                case 3:
                    // Cancel booking
                    System.out.print("Enter Floor Number (1-3): ");
                    floor = sc.nextInt();

                    System.out.print("Enter Room Number (1-5): ");
                    room = sc.nextInt();

                    if(floor >= 1 && floor <= 3 && room >= 1 && room <= 5)
                    {
                        if(rooms[floor - 1][room - 1] == 1)
                        {
                            rooms[floor - 1][room - 1] = 0;
                            System.out.println("Booking Cancelled!");
                        }
                        else
                        {
                            System.out.println("Room Already Available!");
                        }
                    }
                    else
                    {
                        System.out.println("Invalid Room Number!");
                    }
                    break;

                case 4:
                    System.out.println("Exiting System...");
                    break;

                default:
                    System.out.println("Invalid Choice!");
            }

        } while(choice != 4);

        sc.close();
    }
}


Here is the **complete SPPU 2nd Year AIDS OOPL Practical Exam Theory** for your **Hotel Booking System Java program** in an exam-ready format.

---

# ⭐ 1. Problem Statement

The objective of this program is to develop a **Hotel Room Booking System** using Java, where a user can manage hotel room availability. The system allows the user to:

* View the status of rooms (Available or Booked)
* Book a room
* Cancel a booked room
* Exit the system

The hotel is assumed to have:

* **3 floors**
* **5 rooms per floor**

A 2D array is used to store room status:

* `0 → Available room`
* `1 → Booked room`

This program helps in understanding **array usage, loops, decision-making statements, and menu-driven programs in Java**.

---

# ⭐ 2. Concepts Used in This Program (Definitions)

## 1. Class

A class is a blueprint used to create objects. It contains variables and methods.

👉 Example:

```java
class HotelBooking { }
```

---

## 2. main() Method

It is the starting point of a Java program where execution begins.

---

## 3. Object

An object is an instance of a class. Here, `Scanner sc` is an object of Scanner class.

---

## 4. Scanner Class

Used to take input from the user (keyboard input).

👉 Example:

```java
Scanner sc = new Scanner(System.in);
```

---

## 5. Array

An array is a collection of similar data types stored in continuous memory.

### 2D Array:

Used to store data in rows and columns.

👉 Example:

```java
int rooms[][] = new int[3][5];
```

---

## 6. Switch Statement

Used for menu-driven programs to select multiple options based on user input.

---

## 7. Loop (for loop, do-while loop)

* **for loop** → Used for repeated execution (display rooms)
* **do-while loop** → Ensures menu runs at least once

---

## 8. Conditional Statements (if-else)

Used to check conditions like:

* Room available or not
* Valid floor/room number

---

## 9. System.out.println()

Used to display output on the screen.

---

## 10. close() Method

Used to close Scanner object and free system resources.

---

# ⭐ 3. Flowchart (Text Form)

```
START
   ↓
Initialize Scanner and 2D array (rooms[3][5])
   ↓
Display Menu
   ↓
Take user choice
   ↓
 ┌───────────────┐
 │ Choice = 1    │ → Display room status (Available/Booked)
 └───────────────┘
   ↓
 ┌───────────────┐
 │ Choice = 2    │ → Take floor & room input → Book room if available
 └───────────────┘
   ↓
 ┌───────────────┐
 │ Choice = 3    │ → Take floor & room input → Cancel booking if booked
 └───────────────┘
   ↓
 ┌───────────────┐
 │ Choice = 4    │ → Exit system
 └───────────────┘
   ↓
Repeat until Exit
   ↓
END
```

---

# ⭐ 4. Algorithm (Very Detailed Stepwise)

## Step 1:

Start the program.

👉 Purpose: Initialize program execution.

---

## Step 2:

Create a Scanner object to take input from the user.

👉 Purpose: To read floor number, room number, and menu choice.

---

## Step 3:

Declare a 2D integer array `rooms[3][5]`.

👉 Purpose: To store status of hotel rooms:

* 0 → Available
* 1 → Booked

---

## Step 4:

Initialize a loop using **do-while** to display menu repeatedly.

👉 Purpose: To allow continuous interaction until user exits.

---

## Step 5:

Display menu options:

* View Room Status
* Book Room
* Cancel Booking
* Exit

👉 Purpose: Provide user interaction choices.

---

## Step 6:

Take user input for menu selection.

👉 Purpose: To decide which operation to perform.

---

## Step 7:

If user selects **View Room Status**:

* Use nested for loops
* Check each room status
* Display Available or Booked

👉 Purpose: To show current hotel room availability.

---

## Step 8:

If user selects **Book Room**:

* Take floor and room number input
* Validate input range
* Check availability
* Mark room as booked (set value = 1)

👉 Purpose: To book a selected room safely.

---

## Step 9:

If user selects **Cancel Booking**:

* Take floor and room input
* Validate input
* Check if room is booked
* Change status to available (set value = 0)

👉 Purpose: To cancel existing booking.

---

## Step 10:

If user selects **Exit**:

* Terminate loop

👉 Purpose: To stop program execution.

---

## Step 11:

End program and close Scanner.

👉 Purpose: Release system resources.

---

# ⭐ 5. Working of Methods Used

## 1. main() Method

* Entry point of program
* Controls complete execution flow

---

## 2. Scanner Methods

### nextInt()

* Reads integer input from user
* Used for:

  * choice
  * floor number
  * room number

---

## 3. System.out.println()

* Displays messages like menu, status, confirmation

---

## 4. close()

* Closes input stream
* Prevents memory leaks

---

# ⭐ 6. Input-Output Explanation

## 🔹 Input Required:

User provides:

* Menu choice (1–4)
* Floor number (1–3)
* Room number (1–5)

---

## 🔹 Output Displayed:

### Case 1: View Rooms

Displays:

* Floor-wise room status
* Available or Booked

---

### Case 2: Book Room

Displays:

* "Room Booked Successfully!"
  OR
* "Room Already Booked!"

---

### Case 3: Cancel Booking

Displays:

* "Booking Cancelled!"
  OR
* "Room Already Available!"

---

### Case 4: Exit

Displays:

* "Exiting System..."

---

# ⭐ 7. Important Exam Points (Quick Revision)

✔ Uses 2D array (real-world hotel mapping)
✔ Menu-driven program using switch-case
✔ Demonstrates booking system logic
✔ Uses loops + conditional statements
✔ Input validation included
✔ Demonstrates basic system simulation

