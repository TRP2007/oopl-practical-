Polymorphism code
import java.util.Scanner;

class Main{
public static void main(String[] args){

Scanner sc=new Scanner(System.in);

Shape s=null;
String ans;

do{

System.out.println("Choose option: ");
System.out.println("1. Circle");
System.out.println("2. Rectangle");

int choice=sc.nextInt();

if(choice==1){

System.out.println("Enter radius: ");
double r=sc.nextDouble();
s=new Circle(r);

}

else if(choice==2){

System.out.println("Enter length: ");
double l=sc.nextDouble();

System.out.println("Enter breadth: ");
double b=sc.nextDouble();

s=new Rectangle(l,b);

}

else{

System.out.println("Invalid Input");
s=null;

}

if(s!=null){
s.calculateArea();
}

System.out.print("Do you want to continue? (y/n): ");
ans=sc.next();

}while(ans.equalsIgnoreCase("y"));

sc.close();

}
}

interface Shape{
public void calculateArea();
}

class Circle implements Shape{

double radius;

Circle(double radius){
this.radius=radius;
}

public void calculateArea(){
double area=3.14*radius*radius;
System.out.println("Area of circle is: "+area);
}

}

class Rectangle implements Shape{

double length,breadth;

Rectangle(double length,double breadth){
this.length=length;
this.breadth=breadth;
}

public void calculateArea(){
double area=length*breadth;
System.out.println("Area of rectangle is: "+area);
}

}


Here is the **complete practical exam theory (SPPU 2nd Year AIDS – OOPL)** for your **Java Polymorphism program (Shape interface: Circle & Rectangle)** in a clean, exam-ready format.

---

# ⭐ 1. Problem Statement (Simple Explanation)

The aim of this program is to demonstrate **runtime polymorphism in Java** using an **interface named Shape**.

The program allows the user to:

* Choose a shape (Circle or Rectangle)
* Enter required dimensions
* Calculate and display the area

It uses **polymorphism**, where a single reference (`Shape s`) is used to represent different objects (`Circle`, `Rectangle`) at runtime.

---

# ⭐ 2. Concepts Used in This Program (Definitions)

### 1. Class

A class is a blueprint used to create objects.

Example:

```java
class Circle { }
```

---

### 2. Object

An object is an instance of a class.

Example:

```java
Circle c = new Circle(5);
```

---

### 3. Interface

An interface is a blueprint of a class that contains **abstract methods only**.

Example:

```java
interface Shape {
    void calculateArea();
}
```

---

### 4. Polymorphism

Polymorphism means **one name, many forms**.

👉 In this program:

```java
Shape s = new Circle();
Shape s = new Rectangle();
```

Same reference (`Shape`) behaves differently.

---

### 5. Method

A block of code that performs a specific task.

Example:

```java
public void calculateArea()
```

---

### 6. Scanner Class

Used to take input from user.

Example:

```java
Scanner sc = new Scanner(System.in);
```

---

### 7. String

A sequence of characters.

Example:

```java
String ans;
```

---

### 8. Constructor

Special method used to initialize object.

Example:

```java
Circle(double radius) { }
```

---

### 9. Reference Variable

A variable that stores the address of an object.

Example:

```java
Shape s;
```

---

# ⭐ 3. Algorithm (Detailed Stepwise)

### Step 1:

Start the program execution.

### Step 2:

Create a `Scanner` object to accept user input.

### Step 3:

Declare a reference variable of interface type:

```java
Shape s;
```

👉 Purpose: To achieve runtime polymorphism.

---

### Step 4:

Start a loop using `do-while` for repeated execution.

👉 Purpose: To allow user multiple calculations.

---

### Step 5:

Display menu:

* Circle
* Rectangle

👉 Purpose: To take user choice.

---

### Step 6:

Read user choice.

---

### Step 7:

If choice = 1:

* Accept radius
* Create `Circle` object
* Assign it to `Shape s`

👉 Purpose: Dynamic binding

---

### Step 8:

If choice = 2:

* Accept length and breadth
* Create `Rectangle` object
* Assign it to `Shape s`

---

### Step 9:

If invalid choice:

* Display error message
* Set `s = null`

---

### Step 10:

If `s != null`:

* Call `calculateArea()` method

👉 Purpose: Runtime polymorphism (method overriding)

---

### Step 11:

Ask user:
“Do you want to continue?”

---

### Step 12:

Repeat loop if user enters 'y'.

---

### Step 13:

Stop program execution.

---

# ⭐ 4. Flowchart (Text Form)

```
START
  |
  v
Create Scanner object
  |
  v
Declare Shape reference (s)
  |
  v
DO LOOP START
  |
  v
Display menu (Circle / Rectangle)
  |
  v
Read choice
  |
  +----------------------+
  | choice == 1 ?        |
  +----------------------+
        | YES
        v
   Input radius
        |
   Create Circle object
        |
        v
  s = Circle object
        |
        |
        | NO
        v
  +----------------------+
  | choice == 2 ?        |
  +----------------------+
        | YES
        v
   Input length & breadth
        |
   Create Rectangle object
        |
        v
  s = Rectangle object
        |
        |
        | NO
        v
   Display Invalid Input
        |
        v
   s = null
  |
  v
IF s != null → call calculateArea()
  |
  v
Ask continue (y/n)
  |
  v
Repeat or END LOOP
  |
  v
END
```

---

# ⭐ 5. Working of Each Method

---

## 🔹 1. calculateArea() in Circle class

* Takes radius value
* Formula used:

  ```
  area = π × r × r
  ```
* Displays area of circle

👉 Purpose: To calculate circle area using runtime method overriding

---

## 🔹 2. calculateArea() in Rectangle class

* Takes length and breadth
* Formula used:

  ```
  area = length × breadth
  ```
* Displays area of rectangle

👉 Purpose: To calculate rectangle area

---

## 🔹 3. main() method

* Entry point of program
* Handles:

  * Input
  * Object creation
  * Method calling
  * Loop control

---

## 🔹 4. Scanner methods

* `nextInt()` → reads integer input
* `nextDouble()` → reads decimal input
* `next()` → reads string input

---

# ⭐ 6. Input–Output Explanation

---

## 🔹 Input Required:

User provides:

* Choice (1 or 2)
* Radius (if Circle)
* Length & Breadth (if Rectangle)
* Continue choice (y/n)

---

## 🔹 Output Displayed:

### If Circle selected:

```
Area of circle is: 78.5
```

### If Rectangle selected:

```
Area of rectangle is: 50.0
```

### If invalid choice:

```
Invalid Input
```

---

# ⭐ 7. Key Concept (Very Important for Viva)

👉 This program demonstrates:

### ✔ Runtime Polymorphism

Because:

* Same reference (`Shape s`)
* Different objects (`Circle`, `Rectangle`)
* Method decided at runtime

