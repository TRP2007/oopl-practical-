import java.util.Scanner;

class ATM
{
    public static void main(String args[])
    {
        Scanner sc = new Scanner(System.in);

        double balance = 5000;
        int choice;
        double amount;

        do
        {
            System.out.println("\n===== ATM MENU =====");
            System.out.println("1. Check Balance");
            System.out.println("2. Deposit Money");
            System.out.println("3. Withdraw Money");
            System.out.println("4. Exit");

            System.out.print("Enter Your Choice: ");
            choice = sc.nextInt();

            try
            {
                switch(choice)
                {
                    case 1:

                        System.out.println("Current Balance : " + balance);
                        break;

                    case 2:

                        System.out.print("Enter Deposit Amount: ");
                        amount = sc.nextDouble();

                        if(amount <= 0)
                        {
                            throw new IllegalArgumentException("Invalid Deposit Amount");
                        }

                        balance = balance + amount;

                        System.out.println("Amount Deposited Successfully");
                        System.out.println("Updated Balance : " + balance);

                        break;

                    case 3:

                        System.out.print("Enter Withdrawal Amount: ");
                        amount = sc.nextDouble();

                        if(amount <= 0)
                        {
                            throw new IllegalArgumentException("Invalid Withdrawal Amount");
                        }

                        if(amount > balance)
                        {
                            throw new ArithmeticException("Insufficient Funds");
                        }

                        balance = balance - amount;

                        System.out.println("Amount Withdrawn Successfully");
                        System.out.println("Remaining Balance : " + balance);

                        break;

                    case 4:

                        System.out.println("Thank You...");
                        break;

                    default:

                        System.out.println("Invalid Choice");
                }
            }

            catch(ArithmeticException e)
            {
                System.out.println("Exception : " + e.getMessage());
            }

            catch(IllegalArgumentException e)
            {
                System.out.println("Exception : " + e.getMessage());
            }

            finally
            {
                System.out.println("Transaction Completed");
            }

        } while(choice != 4);

        sc.close();
    }
}



Here is the **complete SPPU 2nd Year AIDS OOPL Practical Exam Theory** for your **ATM Java Program** in a clean, exam-ready format.

---

# ✅ **1. Problem Statement**

Design and implement a **Java ATM system** that allows a user to:

* Check account balance
* Deposit money
* Withdraw money
* Exit the system

The program should also handle invalid inputs and errors using **exception handling techniques** such as:

* `ArithmeticException`
* `IllegalArgumentException`

The system ensures that:

* Deposit and withdrawal amounts must be valid
* Withdrawal cannot exceed available balance
* Program runs repeatedly until the user exits

---

# ✅ **2. Concepts Used in the Program (Definitions)**

## 🔷 1. Class

A class is a blueprint for creating objects.
Example:

```java
class ATM { }
```

Here, ATM is a class containing program logic.

---

## 🔷 2. main() Method

* Entry point of Java program
* Execution starts from here

```java
public static void main(String args[])
```

---

## 🔷 3. Scanner Class

Used to take input from user.

```java
Scanner sc = new Scanner(System.in);
```

Example:

* nextInt() → integer input
* nextDouble() → decimal input

---

## 🔷 4. Variables

Used to store data.

* `balance` → stores account balance
* `choice` → stores menu option
* `amount` → stores deposit/withdraw amount

---

## 🔷 5. switch statement

Used for menu selection based on user choice.

Example:

```java
switch(choice)
```

---

## 🔷 6. Exception Handling

Used to handle runtime errors.

### Types used:

### 🔸 ArithmeticException

Occurs when invalid arithmetic operation happens (e.g., withdrawal > balance)

### 🔸 IllegalArgumentException

Occurs when invalid input is given (e.g., negative deposit)

---

## 🔷 7. try-catch-finally block

Used to handle errors safely.

* try → risky code
* catch → handles error
* finally → always executes

---

## 🔷 8. do-while loop

Used to repeat ATM menu until exit.

---

## 🔷 9. System.out.println()

Used to display output.

---

## 🔷 10. double data type

Stores decimal values like money.

---

# ✅ **3. Algorithm (Detailed Steps with Purpose)**

1. **Start program**

   * Purpose: Begin execution of ATM system

2. **Create Scanner object**

   * Purpose: Accept user input from keyboard

3. **Initialize variables**

   * balance = 5000 (starting account balance)
   * choice = menu option
   * amount = transaction value

4. **Start do-while loop**

   * Purpose: Repeatedly show ATM menu until user exits

5. **Display ATM menu**

   * Purpose: Show available banking options

6. **Take user choice input**

   * Purpose: Identify which operation user wants

7. **Enter try block**

   * Purpose: Safely execute banking operations

8. **Use switch-case for choice handling**

   * Purpose: Perform selected operation

9. **Case 1: Check Balance**

   * Display current balance

10. **Case 2: Deposit Money**

* Take amount input
* Check if amount > 0
* If invalid → throw exception
* Add amount to balance
* Display updated balance

11. **Case 3: Withdraw Money**

* Take amount input
* Check if amount > 0
* If invalid → throw exception
* Check if amount > balance
* If yes → throw ArithmeticException
* Subtract amount from balance
* Display remaining balance

12. **Case 4: Exit**

* End ATM session

13. **Default case**

* Show invalid choice message

14. **Catch ArithmeticException**

* Handle insufficient balance error

15. **Catch IllegalArgumentException**

* Handle invalid input error

16. **Finally block executes**

* Print transaction completion message

17. **Loop continues until user enters 4**

18. **Close Scanner**

* Free system resources

19. **Stop program**

---

# ✅ **4. Flowchart (Text Form)**

```
START
  ↓
Initialize balance = 5000
  ↓
Display ATM Menu
  ↓
Take user choice
  ↓
TRY block starts
  ↓
Switch(choice)
  ↓
 ┌───────────────┬──────────────────┬──────────────────┬─────────────┐
 | Case 1        | Case 2           | Case 3           | Case 4      |
 | Check Balance | Deposit Money    | Withdraw Money   | Exit        |
 └───────────────┴──────────────────┴──────────────────┴─────────────┘
  ↓
If error occurs → Catch block
  ↓
Show error message
  ↓
Finally block executes
  ↓
Ask again (loop continues)
  ↓
If choice = 4 → EXIT
  ↓
STOP
```

---

# ✅ **5. Working of Program Functions / Methods**

## 🔷 Scanner Methods

* `nextInt()` → reads integer (menu choice)
* `nextDouble()` → reads decimal value (money amount)

**Purpose:** To take input from user.

---

## 🔷 switch(choice)

* Controls ATM operations
* Executes one block based on user selection

**Purpose:** Menu-based system control

---

## 🔷 throw keyword

Used to manually generate exception.

Example:

* `throw new IllegalArgumentException()`
* `throw new ArithmeticException()`

**Purpose:** Validate banking rules

---

## 🔷 catch block

Handles runtime errors safely without crashing program.

**Purpose:** Prevent program termination due to errors

---

## 🔷 finally block

Always executes after try-catch.

**Purpose:** Confirm transaction completion

---

# ✅ **6. Input and Output Explanation**

## 🔷 Input Required:

* User choice (1–4)
* Deposit amount (case 2)
* Withdrawal amount (case 3)

---

## 🔷 Output Displayed:

### Case 1:

* Current balance

### Case 2:

* Deposit success message
* Updated balance

### Case 3:

* Withdrawal success message
* Remaining balance

### Case 4:

* Exit message

### Error Cases:

* Invalid deposit → "Invalid Deposit Amount"
* Invalid withdrawal → "Invalid Withdrawal Amount"
* Insufficient funds → "Insufficient Funds"

### Finally:

* "Transaction Completed"

---

# ✅ **7. Key Viva Point (Important for Exam)**

* This program demonstrates **menu-driven application**
* It uses **exception handling for banking safety**
* It ensures **data validation before transaction**
* It follows **real-world ATM system logic**
* It is an example of **structured programming with OOP concepts**
